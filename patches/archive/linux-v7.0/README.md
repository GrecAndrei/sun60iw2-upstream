# Historical Linux v7.0 series

These patches are retained only to explain the early port history.

- The series predates the Linux v7.1.3 integration tree.
- `0001-*` has malformed hunk counts and fails an offline `git apply` parse.
- Later patches parsing successfully does not make the sequence reproducible.
- Do not renumber, extend, apply, or cite this directory as the current boot
  baseline.

Regenerate any useful change from the reviewable source and a clean target
kernel instead of repairing this archive in place.
