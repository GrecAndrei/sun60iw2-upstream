# Work-in-progress patches

Files here are isolated drafts, not an ordered or bootable series. Validate a
draft against the intended clean kernel release before moving it into a future
`patches/active/linux-vX.Y/` series.
